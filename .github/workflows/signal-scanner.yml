# DEPRECATED — Moved to short-gravity-web repo
# This workflow is disabled. The active version runs from the web app repo.
name: "[DISABLED] Signal Scanner"

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow has moved to the short-gravity-web repo."
