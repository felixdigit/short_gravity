{
  "name": "short-gravity",
  "private": true,
  "packageManager": "pnpm@9.10.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.8.0",
    "typescript": "^5.0.0"
  }
}
